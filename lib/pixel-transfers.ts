export function pixelTransfers(value:unknown):ArrayBuffer[]{const buffers=new Set<ArrayBuffer>(),seen=new Set<object>();const visit=(v:unknown)=>{if(!v||typeof v!=='object'||seen.has(v))return;seen.add(v);if(ArrayBuffer.isView(v)){if(v.buffer instanceof ArrayBuffer)buffers.add(v.buffer);return}if(v instanceof ArrayBuffer){buffers.add(v);return}for(const item of Object.values(v))visit(item)};visit(value);return [...buffers]}
